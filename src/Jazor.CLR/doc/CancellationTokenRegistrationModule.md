# CancellationTokenRegistrationModule.cs

> ⚠️ **注意**：签名= _+ SHA256Hash(成员)

**成员**：System.Threading.CancellationTokenRegistration.CancellationTokenRegistration()</br>
**签名**：_956101a413714c9c</br>

**成员**：System.Threading.CancellationTokenRegistration.Dispose()</br>
**签名**：_ddfca4a87505c8d8</br>
**注释**：

```xml
<summary>Disposes of the registration and unregisters the target callback from the associated <see cref="T:System.Threading.CancellationToken" />.</summary>
```

**成员**：System.Threading.CancellationTokenRegistration.DisposeAsync()</br>
**签名**：_5ab177c632de03e2</br>
**注释**：

```xml
<summary>Disposes of the registration and unregisters the target callback from the associated             <see cref="T:System.Threading.CancellationToken" />.</summary>
<returns>A task that represents the asynchronous dispose operation.</returns>
```

**成员**：System.Threading.CancellationTokenRegistration.Token.get</br>
**签名**：_3eb82b1d809eca7f</br>

**成员**：System.Threading.CancellationTokenRegistration.Unregister()</br>
**签名**：_3f92a31b30a1bf31</br>
**注释**：

```xml
<summary>Disposes of the registration and unregisters the target callback from the associated             <see cref="T:System.Threading.CancellationToken" />.</summary>
<returns>  <see langword="true" /> if the method succeeds; otherwise, <see langword="false" />.</returns>
```

**成员**：static System.Threading.CancellationTokenRegistration.operator ==(System.Threading.CancellationTokenRegistration, System.Threading.CancellationTokenRegistration)</br>
**签名**：_acc1375b1abd6520</br>
**注释**：

```xml
<summary>Determines whether two <see cref="T:System.Threading.CancellationTokenRegistration" /> instances are equal.</summary>
<param name="left">The first instance.</param>
<param name="right">The second instance.</param>
<returns>  <see langword="true" /> if the instances are equal; otherwise, <see langword="false" />.</returns>
```

**成员**：static System.Threading.CancellationTokenRegistration.operator !=(System.Threading.CancellationTokenRegistration, System.Threading.CancellationTokenRegistration)</br>
**签名**：_5cfb509ee9a8aab9</br>
**注释**：

```xml
<summary>Determines whether two <see cref="T:System.Threading.CancellationTokenRegistration" /> instances are not equal.</summary>
<param name="left">The first instance.</param>
<param name="right">The second instance.</param>
<returns>  <see langword="true" /> if the instances are not equal; otherwise, <see langword="false" />.</returns>
```

**成员**：override System.Threading.CancellationTokenRegistration.Equals(object)</br>
**签名**：_6d73eb424acc37d5</br>
**注释**：

```xml
<summary>Determines whether the current <see cref="T:System.Threading.CancellationTokenRegistration" /> instance is equal to the specified <see cref="T:System.Threading.CancellationTokenRegistration" />.</summary>
<param name="obj">The other object to which to compare this instance.</param>
<returns>  <see langword="true" /> if both this and <paramref name="obj" /> are equal. False, otherwise.     Two <see cref="T:System.Threading.CancellationTokenRegistration" /> instances are equal if they both refer to the output of a single call to the same Register method of a <see cref="T:System.Threading.CancellationToken" />.</returns>
```

**成员**：System.Threading.CancellationTokenRegistration.Equals(System.Threading.CancellationTokenRegistration)</br>
**签名**：_330c3c06bd34b9e4</br>
**注释**：

```xml
<summary>Determines whether the current <see cref="T:System.Threading.CancellationTokenRegistration" /> instance is equal to the specified <see cref="T:System.Threading.CancellationTokenRegistration" />.</summary>
<param name="other">The other <see cref="T:System.Threading.CancellationTokenRegistration" /> to which to compare this instance.</param>
<returns>  <see langword="true" /> if both this and <paramref name="other" /> are equal. False, otherwise.     Two <see cref="T:System.Threading.CancellationTokenRegistration" /> instances are equal if they both refer to the output of a single call to the same Register method of a <see cref="T:System.Threading.CancellationToken" />.</returns>
```

**成员**：override System.Threading.CancellationTokenRegistration.GetHashCode()</br>
**签名**：_9fb481fab54c6699</br>
**注释**：

```xml
<summary>Serves as a hash function for a <see cref="T:System.Threading.CancellationTokenRegistration" />.</summary>
<returns>A hash code for the current <see cref="T:System.Threading.CancellationTokenRegistration" /> instance.</returns>
```
