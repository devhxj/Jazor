# Jazor.Compiler 任务追踪文档

> 更新时间：2026-03-07
> 状态：核心功能完成，架构重构完成，作用域隔离完成，白名单与特性别名检查完成

---

## 一、当前状态概览

### 1.1 核心转换完成度

| 功能模块 | 完成状态 | 测试覆盖 |
|----------|----------|----------|
| 模式匹配 (Pattern) | ✅ | ✅ |
| 循环语句 (Loop) | ✅ | ✅ |
| Switch 语句/表达式 | ✅ | ✅ |
| 字符串插值 (String) | ✅ | ✅ |
| 异常处理 (TryCatch) | ✅ | ✅ |
| 元组 (Tuple) | ✅ | ✅ |
| 创建表达式 (Creation) | ✅ | ✅ |
| 引用操作 (Reference) | ✅ | ✅ |
| 变量声明 (Declaration) | ✅ | ✅ |
| 普通运算 (Ordinary) | ✅ | ✅ |
| 无效操作处理 (Invalid) | ✅ | ✅ |
| 不支持操作 (NotSupport) | ✅ | N/A |
| AST 优化器 (Optimizer) | ✅ | ✅ |

> **测试状态**: 533 个测试全部通过（100% 通过率）

### 1.2 架构改进完成度

| 改进项 | 状态 |
|--------|------|
| Sense 语义上下文 | ✅ 完成 |
| 移除向上遍历 | ✅ 完成 |
| PatternInput 上下文传递 | ✅ 完成 |
| 变量声明提升到块顶 | ✅ 完成 |
| 函数边界隔离 | ✅ 完成 |
| try/catch/finally 体隔离 | ✅ 完成 |
| switch case 体隔离 | ✅ 完成 |

### 1.3 白名单检查完成度

| 检查位置 | 状态 |
|---------|------|
| VisitFieldReference | ✅ |
| VisitPropertyReference | ✅ |
| VisitMethodReference | ✅ |
| VisitInvocation | ✅ |
| GetMapperType | ✅ |
| VisitTypeParameterObjectCreation | ✅ |
| 对象初始化器成员名 | ✅ |
| 模式匹配属性名 | ✅ |
| with 表达式成员名 | ✅ |

---

## 二、待办任务清单

### 🟢 P2 - 中优先级

| 序号 | 任务 | 状态 |
|-----|------|------|
| 1 | 测试覆盖率统计 | ⏳ 待执行 |
| 2 | 注释统一为 XML 文档格式 | ⏳ 待执行 |

### 🟣 P3 - 低优先级

| 序号 | 任务 | 状态 |
|-----|------|------|
| 3 | 性能优化评估 | ⏳ 待评估 |

---

## 三、名称获取规范

### 核心方法

| 方法 | 用途 | 检查顺序 |
|------|------|---------|
| `GetTypeConfigOrWhiteListName(ITypeSymbol)` | 获取类型名称 | 白名单别名 → 特性配置 → 原始名称 |
| `GetConfigOrSymbolName(ISymbol)` | 获取成员名称 | 特性配置 → 原始名称 |
| `GetInitializerMemberName(ISymbol)` | 获取初始化器成员名称 | 白名单别名(setter) → 特性配置 → 原始名称 |
| `GetMethodConfigOrWhiteListName(IMethodSymbol)` | 获取方法名称 | 白名单别名 → 特性配置 → 原始名称 |
| `GetWhiteListExpression(ISymbol, ...)` | 处理白名单操作 | `Alias`/`Inline`/`Import` 操作 |

### 白名单操作类型

| Op 类型 | 处理方式 |
|---------|---------|
| `Alias` | 替换名称 |
| `Inline` | 解析并替换占位符 |
| `Import` | 生成导入调用 |
| `Allowed` | 原生支持 |
| `Discard` | 不支持（抛出异常） |
| `Compile` | 编译器生成处理 |

### 特性配置支持

- `[ECMAScriptName("jsName")]` - 指定 JavaScript 名称
- `[Description("jsName")]` - 作为备选的别名配置

---

## 四、设计原则

### 作用域边界

- `_declarators`（变量声明）→ 不能穿越函数/块边界
- `_specifiers`（import 声明）→ 必须穿越函数边界传播到模块顶层

### 无需白名单检查的操作

| 类型 | 原因 |
|------|------|
| 本地变量/参数引用 | 用户定义的变量 |
| 对象初始化器成员名 | 用户定义的类成员 |
| 元组元素名 | 编译器生成或用户定义 |
| 控制流结构 | 不涉及成员访问 |
| 字符串插值 | 模板字符串转换 |
| 类型检查 (`is`) | 通过 `GetMapperType` 已处理 |

---

## 五、版本历史

### v1.6 - 2026-03-07 (当前)

- 完善白名单与特性别名处理机制
- 所有 533 个测试通过

### v1.5 - 2026-03-06

- 修复 VisitFieldReference 白名单检查
- 完成 Sense 语义上下文重构
- 完成变量声明位置与作用域隔离重构

### v1.0 - 2026-03-03

- 创建开发规则文档
- 确认核心转换功能完成

---

## 六、验收标准

### 转换正确性

- [x] 所有单元测试通过 (533/533)
- [x] 生成的 JavaScript AST 结构正确
- [x] 语义等价性验证通过

### 代码质量

- [x] 构建无警告
- [x] 代码符合项目规范
- [ ] 注释完整清晰 (部分完成)

### 架构质量

- [x] 无向上遍历操作树逻辑
- [x] 语义上下文显式传递
- [x] 函数边界正确隔离作用域
- [x] 变量声明集中提升到块顶
- [x] 所有引用操作都有白名单检查
- [x] 类型/属性/字段/方法名称获取支持白名单和特性别名

---

*本报告最后更新时间：2026-03-07*
