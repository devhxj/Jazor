# RazorVue Playground 支持缺口状态（2026-06-10）

本文是 RazorVue / Playground 的当前边界状态页，不记录逐次修复日志。单次修复过程、focused run 输出和完整历史细节应回到测试名、提交记录、PR 描述和实现代码中追溯。

本文只回答四类问题：

- 当前哪些形态仍必须 fail-fast。
- 当前哪些形态只能保守输出 render-function `.vue`。
- 哪些能力已经进入支持面，不再作为缺口追踪。
- 支持面变化后应从哪里验证。

## 维护规则

- “当前缺口”只放仍有效的限制、降级条件或 fail-fast 条件。
- 缺口关闭后，从“当前缺口”移除；只有它仍帮助理解边界时，才在“已固化能力摘要”保留一行。
- 不在本文追加逐条 focused run 日志。需要证明时，补测试、引用测试名，或在 PR / commit 中记录命令输出。
- 无法证明求值顺序、副作用次数、slot/component metadata、HMR identity、frame identity/depth 或 Vue runtime 语义等价时，继续显式 fail-fast，或保留 render-function `.vue`。

## 不可突破的边界

- RazorVue 的主输出合同是最终 `.vue` / render-function artifact，不引入 wrapper-JS 中间协议。
- C# 表达式、成员、类型引用、CLR helper、import/reference 语义继续走 `Jazor.Compiler` / `SemanticWalker`。RazorVue 只负责 Vue artifact framing、Razor frontend 还原和组件/slot 描述符桥接。
- RazorVue 的 `.razor` 输入必须保持官方 Razor Source Generator 可编译，不能引入只有 RazorVue 自己懂的参数或类型形态。
- Analyzer 可以更早、更严；compiler / RazorVue lowering 仍必须在实际 runtime-sensitive 使用点 fail-fast。

## 支持策略总览

| 领域 | 稳定主线 | 保守策略 | 必须 fail-fast 的核心边界 |
|------|----------|----------|----------------------------|
| Render / Template | 已实现的 root-level 与 mixed/nested template-safe 子集 | 不能证明 template-safe 时保留 render-function `.vue` | async render、`goto`、需要真实 exception/dispose/lock 语义、mutation/byref/loop control 等不可模板编码语义 |
| Template code block / raw markup | template local、受控 assignment / increment / decrement、静态可证明 raw markup | 动态 raw HTML 不降级为普通安全 HTML | 运行时生成 HTML、执行型元素/属性/directive、后续可观察写入 carrier |
| Component / `System.Type` | 可源码证明的静态 `typeof(IVueComponent)` 目标 | 普通 type-token 表达式交回 compiler | 动态 `System.Type` 组件、把 `System.Type` 当普通内容/条件/key/loop source |
| Fragment / Slot carrier | inline template、source-stable carrier、受控 fragment factory 转发链 | 任意 delegate dataflow 不进入 slot 还原路径 | 递归 factory、副作用 dataflow、无法还原匿名模板 body、`ref` / `out` 转发 |
| Render helper / open frame | 只读 captured value、受控 open-frame replay、同 artifact helper class | 无法保持 frame identity/depth 时失败 | caller-owned frame 漂移、跨 helper 未闭合 frame、关闭/重开 caller frame、region 逃逸、写回/逃逸 byref |
| Setup / lifecycle / render control | no-op helper、受控 emit/watch、受控 `ShouldRender` gate | 无源码 base override 或不可证明 delegate identity 时 `FullReloadRequired` 或失败 | async helper、任意外部 invocation、mutation、真实 exception payload、不可证明 delegate escape |
| DOM / route / consumer build | descriptor-aware emits、SFC bridge、常见 route template bridge、colocated consumer handoff | 无法诚实映射的 route 形态拒绝 | 无法映射的 route template |

## 当前缺口

### Render / Template

- 通用 `imperative body -> canonical <template>` 回流仍不支持。当前只接受已实现的 root-level、无副作用、template-safe 窄子集，root/template code-block 内 template-local declaration 前缀后接 guard-return、no-op `do while(false)` / `while(false)`、template-safe `switch`、no-op `try/finally` / empty recovery `try/catch/finally`、`lock(this)` / 受控 readonly object gate、null/default `using` 或 no-op label + 后续 sibling tail，混合模板树中可证明局部且 template-safe 的 `switch` / guard-return / `lock` / null-default `using` / no-op `try/catch/finally` / label / `do while(false)` 子树，以及 typed slot/template fragment 内 guard-return、template-local declaration 前缀后接 guard-return / no-op `do while(false)` / `while(false)` / template-safe `switch` + 后续 template-safe sibling tail、no-op label、null/default `using`、no-op `try/finally` / empty recovery `try/catch/finally`、`lock(this)` / 受控 readonly object gate 的受控回流。其它 imperative subtree 保守输出 render-function `.vue` 或 fail-fast。
- 真正 async render contract 不支持。`await`、`await foreach`、`await using`、`await using var` 不会生成 fire-and-forget async render。
- `goto` 不支持，因为当前 `Jazor.Compiler` 没有任意 jump control flow 的等价 JS lowering。
- 声明式 count-style `for` 只接受可归一到 `__jazorVueForRange(...)` 且不会改变 iterator 求值次数的单 iterator 形态。常量、参数、属性、局部静态 carrier 和其它无副作用 loop-invariant step 可进入声明式 `RazorVueForNode`；多 iterator、非加减 iterator、逐轮 dynamic invocation step、side-effecting step 或 loop-local dependent step 保守进入 imperative loop / render-function。
- 需要 runtime-sensitive exception、真实 dispose、未知 lock target、loop control、mutation、byref、same-artifact helper type runtime declaration 或动态 raw markup 语义的 template recovery 不做静默擦除。

### Template Code Block / Raw Markup

- 声明式模板 code-block 不是通用语句执行模型。除 template local initializer、同一线性声明前缀内一次简单赋值、普通 assignment / increment / decrement 的 imperative render segment recovery、同一 code-block 内受控 callable local invocation，以及已定义结构化控制语句外，delegate/callable template state 不进入 canonical template node。
- 动态 raw markup 不支持运行时生成 HTML。`AddMarkupContent(...)`、`AddContent(..., MarkupString)` 和 Razor template `MarkupString` 只接受源码可分析且可证明为静态 HTML 的子集；静态 carrier / factory / `MarkupString` carrier 的 `+` 拼接仅在每个片段均可证明为静态时支持。
- raw execution 元素、inline `on*`、Vue/raw directive attribute、`srcdoc`、`v-html`、`formaction`、畸形 tag/attribute name，以及可执行 `javascript:` / `vbscript:` / `data:` URL 继续 fail-fast。
- 后续可观察写入的 `RenderFragment` / `MarkupString` / static-markup carrier 不支持；普通 setup `let` carrier 不放宽 source-stable 合同。

### Component / `System.Type`

- 动态 `System.Type` 组件不支持。当前只支持可源码证明最终等于 `typeof(IVueComponent)` 的静态目标，包括 direct `typeof(...)`、source-stable local/member carrier 和受控只读转发链。
- `System.Type` carrier 只能作为 `OpenComponent(Type)` 的静态组件目标使用，不能当普通 render content、attribute、key、condition 或 loop source。
- 普通 CLR/type-token 表达式归 `Jazor.Compiler` / `SemanticWalker` 处理，RazorVue 不提供第二套 type-token lowering。

### Fragment / Slot Carrier

- 任意 `RenderFragment` / delegate dataflow 不支持。只接受 inline template、source-stable local/member carrier、受支持 current-component/local function fragment factory，以及 getter / fragment factory block body 内返回值依赖链可证明只由 source-stable `RenderFragment` local carrier 组成的窄子集。
- current-component fragment factory 之间的非递归只读转发链支持 source-stable 多跳解析，并按调用点/转发点书写顺序保留 captured value scope；任意 delegate 返回/参数传递不进入该路径。
- local function fragment factory 之间的同作用域非递归转发链支持 source-stable 解析，并与 current-component factory 一样保留 captured value scope；跨作用域逃逸或任意 delegate dataflow 不进入该路径。
- recursive fragment factory、getter / fragment factory block body 中无关 local/语句或副作用 dataflow、无法静态还原匿名模板 body 的 callable、fragment factory `ref` / `out` 参数和 by-reference 转发/逃逸继续 fail-fast。
- `in` 只读值参数只在已支持的 captured value 读取场景成立；继续传入任意 by-reference invocation 不支持。

### Render Helper / Open Frame

- render helper 非 builder `ref` 参数仅支持可证明只读的 captured value 读取子集：实参必须是 C# 可寻址值，helper body 不得 assign / increment / decrement、不得通过任何 by-reference invocation 转发，且不提供 caller writeback。`out` 参数、`ref` 写回/逃逸和 by-reference forwarding 继续 fail-fast；`RenderTreeBuilder` 参数必须保持 by-value。
- caller-owned open frame helper 只支持受控 replay：attribute/key/spread mutation、DOM event modifier mutation、slot/default-slot assignment、ambient child emission、expression-bodied single-call helper、builder local alias / `this.` qualified helper 调用、helper-local 平衡 `OpenRegion` / `CloseRegion`，以及能保持 frame identity/depth 的 generic value-erased helper attribute replay、简单条件 attribute/child/event-modifier/key/spread/ambient default-slot child/region ambient default-slot child/implicit default-slot assignment/named-typed slot assignment replay、普通 `if/else` 双分支 attribute/child/default-slot/event-modifier/key/spread/named-typed slot replay、嵌套/组合条件 attribute+child、component attribute+default-slot 和 nested `if/else` replay、guard-return、consecutive guard-return、single-branch terminal-return、both-branches terminal-return replay、caller-owned loop / `try/catch/finally` / `lock` / null-default `using` / null-default using declaration scope 内 frame-neutral attribute/key/spread/event-modifier mutation replay，以及顶层或受控条件分支内的 `switch` single-value/default case、多 single-value label 共享 body、source-stable helper-local prelude guard 和无副作用 pattern/relational/guarded case + explicit terminal `break` replay。frame-neutral `try/catch/finally` replay 可通过 `SemanticWalker` 保留 catch payload、filter、`throw` / rethrow 语义；空 case 标签共享 body 已归入 multiple-label replay；`goto case` / `goto default` 仍按 jump control-flow fail-fast。pattern-local 只允许在 pattern/`when` 条件内部使用；只读 helper-local replay prelude 可被 pattern/`when` 条件读取。case body 依赖 pattern-local、未作为 replay prelude 声明的外部 local-reference、后续写入 local 或副作用 guard 继续 fail-fast。helper 调用必须使用同步 helper，以及显式一对一、按源码/声明顺序一致的参数绑定；generic runtime type-parameter semantics（含已调用 local function / lambda body 内的 `typeof(T)` 等）、`async` helper、omitted optional parameter 和 named argument reshaping 继续 fail-fast。caller-owned loop / `try/catch/finally` / `lock` / null-default `using` / null-default using declaration scope 中改变 frame depth、jump、真实 dispose 语义，以及 `goto` replay 目前没有专用 lowering 协议，无法证明 active frame identity、frame depth、replay order 和 captured value evaluation count 时继续 fail-fast；跨 helper 留未闭合 frame、关闭/重开 caller-owned frame、active frame 漂移或 region 逃逸继续 fail-fast。
- 同文件 helper class lowering 只接受同步、源码可分析、同 artifact module 内的普通 runtime class、static nested helper class，以及 erased value-only generic helper class。generic helper 的静态泛型状态、`typeof(T)` / `new T()` / type-pattern 等 runtime type-parameter 语义继续 fail-fast。helper component 只能通过 `OpenComponent` / component reference 路径渲染；`new Component()` 当普通对象使用继续 fail-fast。

### Setup / Lifecycle / Render Control

- setup/lifecycle lowering 不是通用执行模型。普通 lifecycle no-op helper 仅接受当前组件 private 同步 helper、无副作用实参、普通按值参数、受控只读 `in` 值参数和受控 `params` 展开；`async` helper、`Task` / `ValueTask`、`ref` / `out`、`in` 参数继续 by-reference forwarding、外部 invocation、未知实例 method payload 或超出 `SemanticWalker` statement lowering 的 body 继续失败。
- `SetParametersAsync` 只支持 no-op、base pass-through 和受控 emit/watch 序列、分支、guard-return、普通 `switch`、无 pattern-local 的 pattern switch、含至少一次受支持 callback emit 的受控 loop（含 `await foreach`）、`try/catch/finally` recovery/cleanup 子集。额外 mutation、非 emit loop、声明 pattern-local 并让 case body 依赖其绑定的 pattern switch、任意外部 invocation、真实 exception payload 读取或一般方法体不支持。
- `ShouldRender` 只支持已定义的 no-op、base pass-through、单表达式、受控 control flow、受控 delegate carrier / compare / null-check、local function delegate 参数 identity-return、同源条件分支 / 条件表达式 identity-return、只读本地别名链、必返回的嵌套 block alias-return、同源 `switch` / trailing-return switch identity-return 及同源 `try/catch` identity-return、同步异常分支和纯同步 `throw` 终止方法体。`await foreach`、mutation、任意 delegate escape、跨 member / 外部 callable 传参返回、外部引用程序集无源码 base override 继续 fail-fast 或 `FullReloadRequired`。

### DOM Event / Route / Consumer Build

- component emit modifier 不与 HTML DOM event modifier 共用路径，组件 emits 继续按 descriptor-aware component event lowering。
- route template -> Vue Router bridge 继续拒绝无法诚实映射的长尾形态：optional separator 参数位置非法、SDK Razor route parser 不接受的多层 optional separator composite/mixed segment、catch-all inline constraint、未知自定义 constraint，以及无法表达为“Vue Router path regex + generated metadata 二次校验”的 constraint 组合。普通多参数 composite/mixed segment、普通 catch-all 和单尾部 optional separator composite segment 已由 Emit 回归固化。

## 已固化能力摘要

以下内容已经进入支持面，不再作为当前缺口逐条追踪。具体行为仍以实现和回归测试为准。

| 领域 | 已固化能力 |
|------|------------|
| Body-level imperative render | 常见 `return`、loop、`switch`、`lock`、`try/catch/finally`、`using` / using declaration、无 `goto` labeled statement、局部 mutation、静态 markup / `MarkupString`。 |
| Canonical template recovery | root-level 与 mixed/nested template-safe 子树中的受控 `switch`、guard-return、`try/finally` / 空 recovery、`lock(this)` / 受控 readonly object gate、null/default `using`、no-op label、`do while(false)` / `while(false)`，root/template code-block 内 template-local declaration 前缀后接 guard-return、no-op `do while(false)` / `while(false)`、template-safe `switch`、no-op `try/finally` / empty recovery `try/catch/finally`、`lock(this)` / 受控 readonly object gate、null/default `using` 或 no-op label + sibling tail，以及 typed slot/template fragment 内 guard-return / template-local declaration 前缀后接 guard-return、no-op `do while(false)` / `while(false)` 或 template-safe `switch` + sibling tail / no-op label / null-default `using` / no-op `try/finally` / empty recovery `try/catch/finally` / `lock(this)` / 受控 readonly object gate 子集。 |
| Component / slot metadata | Component parameter descriptor、current-component slot forwarding、builder-style `RenderFragment` / `RenderFragment<T>` slot callback、nested component metadata/import。 |
| Razor IR frontend | mixed attribute、lowercase `class` / `style` fallthrough、DOM event modifier、typed/untyped `RenderFragment` carrier、fragment factory、template local、受控 setup/lifecycle helper payload、动态 `ShouldRender` cached render gate。 |
| Raw markup / `MarkupString` | 可证明静态的 carrier、条件静态分支、静态安全 `+` 拼接、显式 `MarkupString` 表达式入口；plain `string` 仍按普通 render content 处理。 |
| Fragment factory | getter / factory block body 中由 source-stable `RenderFragment` local carrier 组成的返回值依赖链，current-component 与 local function factory 的非递归多跳转发链。 |
| Render helper / open frame | 只读 `ref` captured value 子集、caller-owned attribute/child replay、expression-bodied single-call helper replay、builder local alias / `this.` qualified helper replay、DOM event modifier mutation、generic value-erased helper attribute replay、简单条件 attribute/child/event-modifier/key/spread/ambient default-slot child/region ambient default-slot child/implicit default-slot assignment/named-typed slot assignment replay、普通 `if/else` 双分支 attribute/child/default-slot/event-modifier/key/spread/named-typed slot replay、嵌套/组合条件 attribute+child、component attribute+default-slot 和 nested `if/else` replay、guard-return replay、consecutive guard-return nested replay、single-branch / both-branches terminal-return replay、frame-neutral caller-owned loop / `try/catch/finally` / `lock` / null-default `using` / null-default using declaration scope mutation replay、顶层/受控条件分支内 single-value/default/multiple-label、pattern-local condition-only 与 source-stable helper-local prelude guard 的 pattern/relational/guarded `switch` replay、current-component 与 local function recursive render helper imperative materialization。 |
| Helper class / component boundary | 同文件普通 class、static nested helper class、erased value-only generic helper class runtime 发射；helper component 继续只通过 `OpenComponent` / component reference 路径渲染。 |
| Setup / lifecycle / render control | 普通 lifecycle no-op helper 的只读 `in` 值参数、`SetParametersAsync` 受控 switch / pattern switch / loop emit-watch、`ShouldRender` 同源 delegate identity-return / alias-return / control-flow return 与纯同步 `throw`。 |
| DOM event / build / route bridge | 任意 `on*` 字符串 attribute 不会被当成 DOM event，event modifier metadata 只装饰已确认的 EventCallback / delegate-like DOM event，不提升普通字符串属性；`.vue` default export/import 不作为 Jazor authored module 边界，SFC bridge 会生成 named-export `.mjs`，并重写 default import、default+named import、default re-export 与 selected-entry 相对 `.vue` 依赖闭包；RazorVue library-mode colocated consumer 是同一 ASP.NET Core 项目内的前端消费构建层，`JazorConsumerRoot` 已设置但 runner 缺失时由 MSBuild target fail-fast，不继续产出 stale browser assets；普通多参数 composite/mixed route template bridge、普通 catch-all、单尾部 optional separator composite segment，以及 constrained catch-all / 多层 optional separator / 未知或不可映射 constraint 的稳定 fail-fast 边界。 |

## 验证入口

当前快照的依据应落在测试和实现中，而不是继续扩写本文。涉及 RazorVue 支持面变更时，按风险选择以下入口：

- `dotnet test src/Jazor.RazorVue.Test/Jazor.RazorVue.Test.csproj`
- `dotnet test src/Jazor.RazorVue.RazorIr.Test/Jazor.RazorVue.RazorIr.Test.csproj`
- `dotnet test src/Jazor.EmitTest/Jazor.EmitTest.csproj`
- `git diff --check`

最近状态锚点：

- 2026-06-09：普通 lifecycle no-op helper 的 by-reference 边界已重新收口：value-returning no-op helper 只允许普通按值参数，void no-op helper 继续允许 local-only `in` 只读值读取，但 `ref` / `out`、返回值 helper 的 `in` by-reference 调用、以及 `in` 参数继续 by-reference forwarding 均按 analyzer / generator / pipeline 三层一致 fail-fast；focused by-reference lifecycle 过滤器为 8/8 通过，`Jazor.RazorVue.Test` 全套为 2396/2396 通过。
- 2026-06-09：focused 验证覆盖 Render Helper / Open Frame 的 caller-owned replay、expression-bodied single-call helper replay、builder local alias / `this.` qualified helper replay、DOM event modifier mutation、generic value-erased helper attribute replay、conditional attribute/child/event-modifier/key/spread/ambient default-slot child/region ambient default-slot child/implicit default-slot assignment/named-typed slot assignment replay、普通 `if/else` 双分支 attribute/child/default-slot/event-modifier/key/spread/named-typed slot replay、嵌套/组合条件 attribute+child、component attribute+default-slot 和 nested `if/else` replay、guard-return replay、consecutive guard-return nested replay、single-branch / both-branches terminal-return replay、frame-neutral caller-owned loop / `try/catch/finally` / `lock` / null-default `using` / null-default using declaration scope mutation replay、顶层/受控条件分支内 single-value/default/multiple-label、pattern-local condition-only 与 source-stable helper-local prelude guard 的 pattern/relational/guarded `switch` replay、ordinary conditional child boundary、recursive helper materialization、overload alias 去碰撞，以及 active frame 漂移、recursive caller-owned mutation、component frame DOM event modifier、direct / invoked-local-function / invoked-lambda generic runtime type-parameter semantics、`async` helper、omitted optional parameter、named argument reshaping、case body 依赖 pattern-local、`goto case`、后续写入 helper-local guard、副作用 guard、caller-owned loop / `try/catch/finally` / `lock` / null-default `using` / null-default using declaration scope 改变 frame depth、真实 disposable `using` / using declaration、混合真实 disposable using declaration resource、`goto` / try 内 `goto` 控制流等负向边界；`RazorVueRenderHelperOpenFrameBoundaryTests` 为 150/150 通过，`Jazor.RazorVue.Test` 全套为 2425/2425 通过。
- 2026-06-09：DOM event 判定收紧为属性值必须是 EventCallback / delegate-like；`on*` 字符串 attribute 即使带有 Razor/RenderTree event modifier metadata，也在 SFC template 与 H render-function 路径中保持普通 HTML attribute。focused SFC artifact 事件属性类为 284/284 通过，focused pipeline 事件边界为 6/6 通过，`Jazor.RazorVue.Test` 全套为 2427/2427 通过。
- 2026-06-09：SFC bridge default export/import 边界已补齐回归，覆盖 default import、default+named import、default re-export、render-function-only default export 和 selected-entry 缺失 manifest 相对 `.vue` 依赖 fail-fast；`RazorVueSfcBridgeCompilerTests` 在 `DENOHOST_ALLOW_CHECKSUM_BYPASS=true` 下为 11/11 通过。默认 DenoHost 校验模式当前被本机 `deno.metadata.json` 签名校验失败挡住，既有旧用例同样受影响。
- 2026-06-09：colocated consumer runner 缺失边界已由 SDK integration 固化：`Build_LocalJazorPackage_WebSdkHost_WithColocatedConsumer_MissingRunnerFailsFast` 验证 `JazorEmit` 先完成、MSBuild 报 `Jazor consumer runner was not found`、且不会继续生成 `wwwroot/jazor/client-entry.js` / `.css`。
- 2026-06-09：route template -> Vue Router bridge 长尾边界已补齐 constrained catch-all fail-fast，并确认多层 optional separator composite/mixed segment 停在 SDK Razor route parser 边界；focused route 过滤器在 `DENOHOST_ALLOW_CHECKSUM_BYPASS=true` 下为 8/8 通过。
- 2026-06-09：声明式 count-style `for` 的 step 边界已收紧：`i += ++Step` 等 side-effecting step 不再进入 `__jazorVueForRange(...)` 的单次 step 求值合同，而是按 handwritten / Razor IR frontend、SFC artifact 和 pipeline 一致切到 imperative loop / render-function；focused side-effecting step 为 4/4 通过，正向 count-style step 过滤器为 14/14 通过。
- 2026-06-09：typed child-content / typed slot fragment 内的 standalone template code-block guard-return 和 direct `@if` guard-return + 后续 sibling tail 已进入 canonical template recovery；调用型 guard condition 仍保持 render-function。新增 focused SFC guard-tail 为 3/3 通过，既有 guard-return / conditional return 回归为 `Jazor.RazorVue.Test` 14/14、`Jazor.RazorVue.RazorIr.Test` 23/23 通过。
- 2026-06-09：typed child-content / typed slot fragment 内 no-op labeled statement、null/default `using` statement / using declaration + 后续 template-safe sibling tail 已进入 canonical template recovery；真实 disposable using declaration、label body mutation 仍保持 render-function。新增 focused label/using tail 为 6/6 通过，既有 BuildRenderTree label/default-using 回归为 16/16，Razor IR conditional/using 回归为 44/44 通过。
- 2026-06-09：typed child-content / typed slot fragment 内 no-op `try/finally`、empty recovery `try/catch/finally`、`lock(this)`、受控 readonly object gate + 后续 template-safe sibling tail 已确认进入 canonical template recovery；catch payload 与未知 lock target 仍保持 render-function。新增 focused try/lock tail 为 6/6 通过。
- 2026-06-09：typed child-content / typed slot fragment 内 no-op `do while(false)` / `while(false)` 和 template-safe `switch` + 后续 template-safe sibling tail 已确认进入 canonical template recovery；`continue`、side-effecting switch value 和 declaration-pattern case body 仍保持 render-function。新增 focused loop/switch tail 为 6/6 通过，Razor IR root switch/do-while 相邻回归为 6/6，BuildRenderTree switch/do-while/while 相邻回归为 15/15 通过。
- 2026-06-09：typed child-content / typed slot fragment 内 template-local declaration 前缀后接 guard-return、no-op `do while(false)` 或 template-safe `switch` + 后续 template-safe sibling tail 已确认进入 canonical template recovery；`throw` 与 `continue` 仍保持 render-function。新增 focused template-local tail 为 5/5 通过，typed slot loop/switch 和 try/lock 相邻回归各为 6/6，root/template-local 相邻回归为 9/9，parity template-local tail 回归为 6/6，BuildRenderTree switch/do-while/while 相邻回归为 89/89。
- 2026-06-09：root/template code-block 内 template-local declaration 前缀后接 no-op `do while(false)` / `while(false)` 或 template-safe `switch` + 后续 sibling tail 已确认进入 canonical template recovery；`continue` 与 invocation switch value 仍保持 render-function。新增 focused root template-local loop/switch tail 为 5/5 通过，typed slot template-local tail 相邻回归为 5/5，root/template-local 与 parity 相邻回归合计 15/15，typed slot loop/switch/try-lock 相邻回归为 12/12，BuildRenderTree switch/do-while/while 相邻回归为 89/89。
- 2026-06-10：root/template code-block 内 template-local declaration 前缀后接 no-op `try/finally` / empty recovery `try/catch/finally`、`lock(this)` / 受控 readonly object gate、null/default `using` statement / using declaration 或 no-op label + 后续 sibling tail 已确认进入 canonical template recovery；catch payload、未知 lock target、真实 disposable using declaration、读取被擦除 using local 与 label body mutation 仍保持 render-function。新增 focused root try/lock/using/label tail 为 12/12 通过，root loop/switch 相邻回归为 5/5，typed slot try/lock/using/label 相邻回归为 12/12，`Jazor.RazorVue.Test` 的 `Try|Lock|Using|Label` focused 面为 282/282。
- 2026-06-10：caller-owned generic helper 的 runtime type-parameter semantics 边界已按 direct helper、已调用 local function 和已调用 lambda body 固化；`typeof(T)`、`default(T)`、`new T()`、`is T` / declaration pattern 继续 fail-fast，未调用 local function / lambda body 不做过度扫描。新增 focused generic helper 边界为 10/10 通过，`RazorVueRenderHelperOpenFrameBoundaryTests` 为 156/156 通过。
- 2026-06-10：caller-owned async helper fail-fast 边界已按 current-component method 与 local function helper 固化；async void、async `Task` 和 async `ValueTask` 均明确报 async / synchronous replay 边界，不误归类为普通 non-void helper。新增 focused async helper 边界为 5/5 通过。
- 2026-06-10：caller-owned helper omitted optional parameter 和 named argument reshaping 边界已固化；声明顺序一致的显式命名调用仍可 replay 并保持 captured value 一次求值，命名参数重排、omitted optional default value、以及未读取的 omitted optional 参数继续 fail-fast。新增 focused optional/named helper 边界为 5/5 通过。
- 2026-06-10：caller-owned `switch` replay 长尾边界已复核并补齐；pattern-local 仅允许在 pattern/`when` 条件内部使用，case body 依赖 pattern-local、未纳入 helper replay prelude 的外部 local-reference、后续写入 local、side-effect guard、`goto case` / `goto default` 继续 fail-fast。新增 local-function 外部 local guard 与 `goto default` 回归，focused switch 边界为 8/8 通过。
- 2026-06-10：caller-owned frame identity/depth 稳定性边界已按 current-component helper 与 local function helper 对称固化；跨 helper 未闭合 frame、关闭 caller frame、active frame 漂移、region 逃逸、component frame DOM event modifier 和 recursive caller-owned mutation 继续 fail-fast。新增 local function 对称负向矩阵，focused frame 边界为 15/15 通过。
- 2026-06-10：caller-owned runtime control-flow / dispose-aware replay 边界已按 current-component helper 与 local function helper 对称固化；loop / `try/catch/finally` / `lock` / null-default `using` / null-default using declaration scope 内 frame-depth mutation、try 内 `goto`、真实 disposable `using` / using declaration 继续 fail-fast。新增 local function 对称负向矩阵，focused runtime control-flow 边界为 16/16 通过。
- 2026-06-10：Fragment / Slot Carrier 的 fragment factory by-reference 边界已按 current-component 与 local function factory 固化；`ref` / `out` 参数继续 fail-fast，`in` 只读参数可作为 value capture 使用，但继续 by-reference forwarding / escape 必须 fail-fast。新增 focused factory by-reference 矩阵为 7/7 通过。
- 2026-06-10：Fragment / Slot Carrier 的 current-component member getter、current-component factory 与 local function factory block-body 返回链已按 source-stable `RenderFragment` local carrier 规则固化；getter / factory block body 中无关 carrier、side-effect 前缀、任意 delegate dataflow、以及通过 callable 返回匿名模板 body 的形态继续 fail-fast。新增 focused canonical fragment carrier 矩阵为 14/14 通过。
- 2026-06-10：Fragment / Slot Carrier 的入口级同构验证已补齐 BuildRenderTree frontend 与 Razor IR frontend 两条路径；current-component member getter / factory、local function factory block-body source-stable 返回链保持结构化 template/slot 还原，delegate dataflow、callable anonymous-template body 与 recursive factory 继续 fail-fast。新增 focused frontend fragment carrier 矩阵为 12/12 通过。
- 2026-06-10：Fragment / Slot Carrier 的 artifact / pipeline 端到端验证已补齐；SFC artifact 覆盖 current-component member getter、current-component factory 与 local function factory block-body source-stable 返回链的 template `.vue` 输出，pipeline 覆盖同形态 `.mjs` render code 内联输出，delegate dataflow、delegate invocation、callable anonymous-template body 与 recursive factory 继续 fail-fast。新增 focused SFC artifact / pipeline fragment carrier 矩阵为 14/14 通过。
- 2026-06-17：Jolt host SDK 对齐收口。前述 `global.json` 指向 `11.0.100-preview.5` SDK、`Directory.Build.props` 的 `CodeAnalysisVersion` 同步到 `5.8.0-1.26302.115` 之后，`RazorProjectEngine.ProcessDesignTime(...)` 在 Razor SDK 10.4 已移除并统一为 `Process(...)`；本次发现该迁移此前只覆盖了 `Jazor.RazorVue.RazorIr.Test` 的两个测试 call site，漏掉了 Jolt host 生产代码的唯一 call site `src/Jolt/Razor/InProc/RazorDesignTimeCodeProjectionService.cs`，导致 Jolt host 与 `Jolt.Test` 无法编译。已将该生产 call site 一并迁移到 `Process(...)`，并修复两份重复的 `GetProjectedComponentTypeName` 测试 helper（`JoltInProcRoslynTests.cs` + `JoltLspTests.cs`）：SDK 10.4 生成的 partial class 名从基于文件名（如 `SharedSource`）改为哈希名 `AspNetCore_<hash>`，旧 helper 用 `"Shared"` 子串锚定 class/namespace 位置会被 `#pragma checksum` 头中的文件名误命中，已改为直接取生成的 namespace + partial class 最后一个匹配。全量四套均绿：`Jazor.RazorVue.Test` 2531/2531、`Jazor.RazorVue.RazorIr.Test` 667/667、`Jazor.EmitTest` 201/201、`Jolt.Test` 778/778（Jolt 此前 5 个 references/rename LSP 测试失败，现已修复）。
- 2026-06-16：DOM Event / Route / Consumer Build 盘点收口。该领域大部分在 2026-06-09 已固化：DOM event modifier（static / dynamic / merged / string-attribute / component-frame 负向）在 frontend / IR / SFC / pipeline 四层均有 witness；route template -> Vue Router bridge 的正向路径（普通多参数 composite/mixed segment、普通 catch-all、单尾部 optional separator composite segment）由 Emit 回归固化，长尾负向形态停在 SDK Razor route parser / Emit fail-fast 边界；SFC bridge default export/import 由 `RazorVueSfcBridgeCompilerTests` 固化。本次仅补一条此前未单独见证的层契约：`@onclick:preventDefault="false"`（常量 `false` / cleared modifier）在 `.razor` / template 路径（Razor IR frontend + SFC artifact）是编译期 no-op——IR frontend 不记录 `EventModifiers.PreventDefault`，SFC template 直接 emit 裸 handler 引用、无 modifier gate；这与 pipeline 的 `AddEventPreventDefaultAttribute(..., false)` runtime `setEventModifier(..., false)` replay 形态是有意的分层差异（template 路径静态可证明为 no-op，pipeline 路径保留 runtime replay 保真）。新增 Layer 2 + Layer 3 cleared-modifier witness 为 2/2 通过；全量 `Jazor.RazorVue.RazorIr.Test` 为 667/667 通过（RazorVue 项目无变更，仍为 2531/2531）。
- 2026-06-16：Setup / Lifecycle / Render Control 的四层一致性盘点已补齐。该领域是 imperative-only 特性：Layer 2（Razor IR frontend `.razor`）天然不适用（`SetParametersAsync` / `ShouldRender` 是命令式 override），实际只有 BuildRenderTree frontend（Layer 1，经 `ESGeneratorTests`）、SFC artifact（Layer 3）、pipeline（Layer 4）三层。此前 pipeline 已极厚（`SetParametersAsync` 69 个、`ShouldRender` 95 个），Layer 1 也充分；Layer 3（SFC artifact）最薄：`SetParametersAsync` 只有 1 条 witness（base+multi-emit 顺序），`ShouldRender` 有 7 条（已覆盖 dynamic / local-prefixed / local-mutation / if-else / switch / pattern-local / recursive-pattern 的 cached-render-gate）。本次在 Layer 3 补齐 3 条核心 `SetParametersAsync` emit-watch 正向 witness（conditional emit、switch emit、`await foreach` emit），全部 lower 成 `watch(() => [props.x], async () => { ... })` script-setup 形态，并按源码顺序保留 emit 顺序。新增 focused `SetParametersAsync` SFC 面为 4/4 通过；全量 `Jazor.RazorVue.Test` 为 2531/2531 通过（IR 项目无变更，仍为 665/665）。`ShouldRender` 与 no-op helper 在各层的覆盖已充分，不再追加。
- 2026-06-16：Render Helper / Open Frame 的四层一致性盘点已补齐。该领域是 imperative-only 特性：Layer 2（Razor IR frontend `.razor`）天然不适用（render helper 是 `BuildRenderTree` 命令式 helper 方法），因此实际只有 BuildRenderTree frontend（Layer 1）、SFC artifact（Layer 3）、pipeline（Layer 4）三层。此前 boundary 类 `RazorVueRenderHelperOpenFrameBoundaryTests` 有 178 个测试（Layer 1=106、Layer 3=8、Layer 4=64），但 Layer 3（SFC artifact）只有 ref-parameter helper、recursive helper、generic helper class 等少量 witness，caller-owned replay 的正向路径（attribute mutation、child emission、conditional child、guard-return）完全没有 SFC 见证。本次在 Layer 3（`RazorVueSfcArtifactFactory`）补齐了 4 条核心 caller-owned replay 正向 witness（无 captured value 的 child emission 走 template carrier；带 captured binding 的 expression-bodied attribute mutation、conditional child、guard-return attribute 走 render-function carrier），并澄清了 caller-owned replay 在 SFC artifact 端的 carrier 选择：无 captured value 时落在 template，带 captured binding 时落在 render-function。新增 focused Render Helper / Open Frame 矩阵：boundary 类为 182/182 通过；全量 `Jazor.RazorVue.Test` 为 2528/2528 通过（IR 项目无变更，仍为 665/665）。
- 2026-06-16：Fragment / Slot Carrier 的四层一致性盘点已补齐。正路径（inline template、source-stable local/member `RenderFragment` carrier、current-component / local function fragment factory、getter / factory block body 返回链、多跳非递归转发链、`in` 只读值参数作为 captured value）在 2026-06-10 已基本覆盖；本次补齐的是 `ref` / `out` / `in`-by-reference 参数工厂负向边界在各层的不对称缺口：Layer 2（Razor IR frontend `.razor`）此前完全没有 `ref` 参数 fragment factory 负向证据（只有 `out`），`in`-by-reference forwarding/escape 只在 frontend 有证据；Layer 4（pipeline）对 `out` 参数 fragment factory 缺少两侧证据（只有 frontend）。本次新增：Layer 2 `ref` 参数 fragment factory 负向 trio（frontend + SFC + pipeline）、Layer 1 + Layer 2 `out` 参数 fragment factory 负向 pipeline、Layer 2 `in`-by-reference forwarding/escape 负向 SFC + pipeline，证明 fragment factory 的 `ref` / `out` / `in`-by-ref 参数与 by-reference escape 在四层一致 fail-fast。新增 focused Fragment / Slot 矩阵：RazorVue focused 面为 117/117，IR focused 面为 39/39 通过；全量 `Jazor.RazorVue.Test` 为 2524/2524，`Jazor.RazorVue.RazorIr.Test` 为 665/665 通过。
- 2026-06-16：Component / `System.Type` 的四层一致性盘点已补齐。此前 BuildRenderTree frontend（Layer 1）只有 attribute 一条 runtime-value 负向证据，且 SFC artifact（Layer 3）只有 local-carrier 一条正向证据；forwarding chain 正向只在 pipeline（Layer 4）有证据。本次在 Layer 1（`BuildRenderTreeTemplateFrontend.CreateRenderTree`）补齐了 System.Type carrier 被当作 content / key / condition / loop source 的负向证据与 local-forwarded-from-member、member forwarding chain 两条正向证据，在 Layer 3（`RazorVueSfcArtifactFactory`）补齐了 direct `typeof(IVueComponent)`、property carrier、readonly field carrier、local-forwarded-from-member、member forwarding chain 五条正向证据，证明共享 `RazorVueComponentTypeCarrierHelper` 的 `ThrowIfComponentTypeCarrierUsedAsRuntimeValue` / `TryResolveComponentTypeCore` 在多层一致执行。Layer 2（Razor IR frontend）的 `OpenComponent(Type)` 正向路径保持空缺是有意为之：`.razor` 输入通过 `OpenComponent<T>` 泛型形态渲染 `<Component/>`，`System.Type` carrier 在该路径只作为 runtime-value 负向 fail-fast。新增 focused Component / `System.Type` 矩阵：RazorVue focused 面为 34/34，IR focused 面为 8/8 通过；全量 `Jazor.RazorVue.Test` 为 2523/2523，`Jazor.RazorVue.RazorIr.Test` 为 659/659 通过。
- 2026-06-16：Template Code Block / Raw Markup 的四层一致性盘点已补齐。Razor IR frontend（`.razor` 输入）此前对 executable URL（`javascript:` / `vbscript:`）、`formaction`、malformed element/attribute name 三条 fail-fast 边界只有单层或缺失证据，静态拼接 carrier 正向路径也缺少独立证据；本次在 Layer 2（Razor IR frontend `CreateRenderTree`）、Layer 3（SFC artifact `RazorVueSfcArtifactFactory`）、Layer 4（pipeline `RazorVuePipeline` / `RazorVueArtifactFactory`）补齐了 executable URL / vbscript / formaction / malformed element & attribute name 的负向 trio 与静态拼接 `string` carrier → `(MarkupString)` cast 的正向 trio，证明共享 `RazorVueStaticMarkupParser` 在四层一致 fail-fast。新增 focused raw-markup 矩阵：IR frontend focused 面为 66/66，RazorVue focused 面为 129/129 通过；全量 `Jazor.RazorVue.RazorIr.Test` 为 659/659，`Jazor.RazorVue.Test` 为 2512/2512 通过。注：本机 `global.json` 已指向 `11.0.100-preview.5` SDK（Razor Compiler 10.4 / Roslyn 5.8），`Directory.Build.props` 的 `CodeAnalysisVersion` 同步到 `5.8.0-1.26302.115`，`RazorProjectEngine.ProcessDesignTime(...)` 在 SDK 10.4 已移除并统一为 `Process(...)`；IR-document inventory 与 `@bind` probe 测试已按新 SDK 的 IR 形状（静态 `<p>` 折叠为 `MarkupBlockIntermediateNode`、代码 token 去前导换行、无 tag-helper 时 `@bind` 落为 raw markup）同步。
- 2026-06-10：Render / Template 的 async render / jump control-flow 边界已按 BuildRenderTree frontend、Razor IR frontend / 官方 Razor SG 输入边界、SFC artifact 和 pipeline 四层补齐验证；`goto` 在 frontend 保留为 imperative method body 并在 artifact / pipeline 层按 `UnsupportedImperativeRenderLowering` fail-fast，`await`、`await foreach`、`await using` / `await using var` 不会生成 fire-and-forget async render，Razor IR `.razor` 形态中 async render 停在官方 Razor SG `CS4033` 编译边界。新增 focused async / jump 矩阵为 24/24 通过。
- 2026-06-09：focused 验证覆盖 Razor IR / BuildRenderTree Fragment factory returned-value dependency、getter local-chain、`MarkupString` / static-markup 表达式入口收紧、later-write 负向边界。
- 2026-05-29：Emit 全套在 SDK integration 编排优化后为 196/196 通过；同日补充普通多参数 composite/mixed route bridge 回归。
- 2026-05-29：focused 验证覆盖 count-style `for`、Template code-block imperative segment、mixed/nested template-safe recovery、raw markup 静态条件/拼接、render helper 只读 `ref`、helper class、setup/lifecycle 和 `ShouldRender` 受控边界。

## 下一步

- 当前状态：所有“当前缺口”领域（Render / Template、Template Code Block / Raw Markup、Component / `System.Type`、Fragment / Slot Carrier、Render Helper / Open Frame、Setup / Lifecycle / Render Control、DOM Event / Route / Consumer Build）已完成四层一致性盘点，现有正向 / 负向边界在各层的行为均由测试名、commit 描述和 git 历史佐证。本页不再主动追加盘点任务。
- 维护姿态：后续新发现的缺口只补充到“当前缺口”，并在“最近状态锚点”记录补齐过程；某层因 SDK 升级或实现演进出现 IR-shape / carrier 选择漂移时，按本页“维护规则”更新对应测试与锚点，不回退到逐次修复日志。下一个具体任务由新发现的缺口或实现演进触发，而不是本文预设。
- 后续新发现的缺口只补充到“当前缺口”；完成过程留在测试名、PR/commit 描述和 git 历史中。
