# RazorVue 组件封装原则

> Status: active
> Scope: `ECMAScript.VueContract`、组件库 binding 与 RazorVue lowering。

## 目标

组件库的 C# API 必须优先使用标准 C# 和 Razor 语义，而不是在其上另建 Vue DSL。
Authoring 时首先使用 `[Parameter]`、`EventCallback`、`RenderFragment`、`record`、原生 `union`、集合初始化器和 C# 命名约定。最终 ECMAScript 名称遵循 [compiler/ECMAScriptNamingPolicy.md](../compiler/ECMAScriptNamingPolicy.md)：`Description` 未用于正常说明时推荐使用成员级 `[Description("@#...")]`；`ECMAScriptName` 可随时直接使用，并在 `Description` 已用于说明时承载专用名称；普通成员不再依赖隐式 PascalCase 转小驼峰。

组件库的公开 authoring 类型沿用其组件前缀而不重复命名空间品牌：Element Plus 使用 `El*`，TDesign 使用 `T*`。仅 npm 根入口保留 `ElementPlus`、`TDesign` 完整品牌名；不保留旧长前缀别名，避免双重 API 表面。

## 最小合同

| 需求 | 默认 C# 表达 | 允许的例外元数据 |
| --- | --- | --- |
| 组件 prop | `[Parameter]` 属性，默认保留 C# 源名称 | 推荐 `[Description("@#modelValue")]`；`[ECMAScriptName("modelValue")]` 也可直接使用，且适用于 `Description` 已用于说明的成员 |
| 普通事件 | `EventCallback` / `EventCallback<T>`；listener key 默认保留 C# 源名称 | listener key 不同时在属性上声明名称；Jazor 不声明或反推 raw emit |
| 双向绑定 | `Value` + `ValueChanged`、`ModelValue` + `ModelValueChanged` 或 `X` + `XChanged` 仅是 C# authoring 习惯 | `modelValue`、`onUpdate:modelValue` 分别显式声明；不从成员配对推导 |
| 默认 slot | `RenderFragment` 参数，默认保留 C# 源名称 | 要使用 Vue `default` 时，在成员上声明 `[Description("@#default")]` 或 `[ECMAScriptName("default")]` |
| 普通 slot | `RenderFragment` 参数，默认保留 C# 源名称 | 在属性上用 `[Description("@#header.data-table-select")]`；`Description` 已占用时使用 `ECMAScriptName` |
| 对象值 | `record` | 无，结构化 record 在 JS 中擦除为对象 |
| 多分支值 | 原生 `union` | 分支类型可赋值重叠且必须保留精确 `AsX` 语义时使用 tagged fallback |
| 未建模 prop | `AdditionalAttributes` | 此是唯一公开 `object?` sink，不能借此弱化已知的强类型 API |
| npm 导入 | 无 C# 等价语法 | 类级导入标识，同时提供 specifier 与 export 名 |

## 现行元数据

| 元数据 | 生产 consumer | 规则 |
| --- | --- | --- |
| `VueLibraryComponent` | analyzer 组件识别、RazorVue named import lowering | 仅承载 `ImportSpecifier` 与 `ExportName`；C# 无法表达的 npm specifier 与 named export 只在此处声明 |
| `VueInject` | RazorVue contract-to-implementation 解析 | 仅在程序集级声明容器 contract 与实现组件的编译期映射；实现必须保持 contract 的参数与类型约束 |
| `Description("@#...")` / `ECMAScriptName` | compiler 与 RazorVue 成员名称映射 | 绑定具体 Roslyn 成员；前者是未占用 `Description` 时的推荐写法，后者不受使用场景限制；解析冲突时后者优先 |

## 资源封装

每个组件库 NuGet 包携带 browser ESM、CSS、静态资源、许可证和 `jazor/<library>/manifest.json`。`manifest.json` 是逻辑 import、依赖版本和样式顺序的唯一资源描述；`Jazor.Emit` 在 debug 与 release 共用的物化阶段把它们复制为应用本域的 `vendor/<library>/<version>/` 资源、import map 和 style list。

组件类型只描述 JavaScript named import，不描述样式。应用和封装库均不需要 `node_modules`、npm、CDN 或运行时远程下载。

## 不变约束

- 生产输入仍是官方 Razor SG 生成 C#；不回退到自定义 Razor 解析或 wrapper-JS 协议。
- 最终 ECMAScript 名称必须由 Roslyn 已绑定的成员及统一名称合同决定；RazorVue 只做结构 lowering，不得从 `ChildContent`、`DefaultContent`、`Changed`、`OnX`、`Content`、PascalCase、camelCase 或 kebab-case 推导名称。
- 派生组件若要改变继承参数的 Vue 名称，必须显式声明 `new [Parameter]` 并把通用名称元数据标在新属性上；未隐藏的继承参数继续使用基类符号及其名称。
- 异常 Vue 名称和点号 slot 必须绑定到具体成员符号并有可测试的通用元数据路径；动态 slot 必须同时具备强类型 C# API 与产物 lowering，不能只保留声明。
- 不为兼容旧 DSL 增加新的 compiler feature；已退役的公开表面在迁移后视为完全移除。
