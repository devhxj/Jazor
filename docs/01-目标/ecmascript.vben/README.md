# ECMAScript.Vben

`src/ECMAScript.Vben/` 的目标文档索引。  
这里聚焦后台框架壳层的 authoring 边界、UI 适配分层，以及与 `ECMAScript.Vue3` / `ECMAScript.TDesign` / 未来 `ECMAScript.ElementPlus` 的协作方式。

## 文件索引

| 文件 | 说明 |
|------|------|
| `vben-balanced-design.md` | `ecmascript.vben` 的平衡式目标设计、分层边界与第一阶段收口策略 |
| `vben-abstract-architecture.md` | `Vben` 的正式抽象架构，明确不把第三方组件库适配纳入主产品线 |
