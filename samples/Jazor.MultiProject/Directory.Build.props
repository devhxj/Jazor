<Project>
  <PropertyGroup>
    <!-- Keep in sync with src/Jazor/Jazor.csproj <Version>. build-local.cs reads from Jazor.csproj and passes via -p:. -->
    <JazorPackageVersion Condition="'$(JazorPackageVersion)' == ''">0.1.13</JazorPackageVersion>
    <LangVersion>preview</LangVersion>
  </PropertyGroup>
</Project>
