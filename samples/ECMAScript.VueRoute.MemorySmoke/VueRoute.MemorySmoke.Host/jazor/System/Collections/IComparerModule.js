import { compareObjectsCore, ensureComparerInstance } from "System/Collections/Generic/ComparerT1Module.js";
export function _7dffdd7244581cc5(instance, x, y) {
  ensureComparerInstance(instance);
  return compareObjectsCore(x, y);
}
