import { compareCore, ensureComparerInstance } from "System/Collections/Generic/ComparerT1Module.js";
export function _0289dcf579b8a65e(instance, x, y) {
  ensureComparerInstance(instance);
  return compareCore(x, y);
}
